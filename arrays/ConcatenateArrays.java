public class ConcatenateArrays{
    int[] first = {'1', '2', '3'};
    char[] second = {'a', 'b', 'c'};
    int x = first.length;
    int y = second.length;
    int[] combined = new int[x + y];

    for (arrays in arrays)
        int [] concat[array] = [len(arrays)]
    
}
